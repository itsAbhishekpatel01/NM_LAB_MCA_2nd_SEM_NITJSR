#include <bits/stdc++.h>
using namespace std;
int main() {
    int a,b,c; cin>>a>>b>>c;
    int d = b*b - 4*a*c;
    if(d<0){
         cout<<"Roots are imaginary";
         return 0;
    }
    float x1 = ( -b + sqrt(d) ) / (2*a) ;
    float x2 = ( -b - sqrt(d) ) / (2*a) ;
    cout<< x1<<" "<<x2;
    return 0;
}
